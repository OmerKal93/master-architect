"""The static analyzer and CLI.

- ``agent_reliability.lint.frontend`` — the safe Python frontend (E03): discovery, resource
  limits, and micro-IR lowering.

The CLI and the first rule (AR001) land in E04. See ``EXECUTION.md``.
"""
